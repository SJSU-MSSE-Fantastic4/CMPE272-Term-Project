_format_version: "3.0"
_transform: true

services:
  - url: http://host.docker.internal:5000
    name: post-service
    routes:
      - name: post-route
        paths:
          - /posts
  - url: http://host.docker.internal:3000
    name: follow-service
    routes:
      - name: follow-route
        paths:
          - /user
